{
        "name": "PlayWright with python",
        "postCreateCommand": "python -m venv .venv && . .venv/bin/activate && pip install --upgrade pip && pip install playwright && playwright install --with-deps", 
}
